# Fullhouse engineering assistant

Approved in chat: persistent Russian communication, English code comments, repository and deployment tools, server workspace, verified outcomes. Initial scope is info14fourteen-creator/ful-house, Heroku fulhouse-ai, Cloudflare Pages ful-house, owned Runpod worker. Expand through explicit project configuration later.

Model credentials must not enter conversations or unprivileged command environments. Broker tools require the authenticated admin, scope service requests to the named resources, cap output/time, and use pinned SSH. Workspace persists on the existing network volume and executes as a non-root user. GitHub Actions performs deployments with repository secrets. Infrastructure power control remains under the existing controller, which counts active tool jobs before idle shutdown.

Rules and tools attach to the existing model so saved conversations work. Validate direct tool invocations and an actual model tool roundtrip. Measure startup before claiming speed gains; retain 600-second GPU idle policy and hourly cap. Existing authorization covers deployment and GitHub pushes. Permanent Cloudflare API token creation is awaiting the separate browser-required confirmation.
